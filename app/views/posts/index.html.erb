<div class="main posts-index">
  <div class="container">
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        <h1 class="display-4">投稿一覧</h1>
        <p class="lead">ここには新規投稿で行われた投稿が表示されます。</p>
        <p class="lead">まだ、ユーザー機能が実装されていないので、誰の投稿かは区別できません.</p>
      </div>
    </div>
    <% if flash[:notice] %>
      <div class="alert alert-success" role="alert">    
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= link_to '/posts/new' do %>
      <button type="button" class="btn btn-primary">新規投稿</button>
    <% end %>
    <% @posts.each do |post|  %>
      <div class="media">
        <img src="<%= "/user_images/#{post.user.user_image}" %>" width="64px" class="mr-3" alt="...">
        <div class="media-body">
          <h5 class="mt-0"><%= post.user.name %></h5>
          <%= link_to(post.content, "/posts/#{post.id}/show") %>
        </div>
      </div>

    <% end %>
    <div>
      <%= link_to '/posts/new' do %>
      <button type="button" class="btn btn-primary">新規投稿</button>
      <% end %>
    </div>
  </div>
</div>